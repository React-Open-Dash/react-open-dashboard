import React from 'react';
import PropTypes from 'prop-types';

/**
 * TODO: Develop this component
 * REQUIREMENT: This component must be created following the designs described in https://www.figma.com/file/lfO8GTUs6EUmD5cRCDFx0z/Untitled?type=design&node-id=11-55&t=tWEDyjDkH7T1UQk0-4
 * REQUIREMENT: this component must supoport al the types described in the figma file, this includes;
 *      - h1
 *      - h2
 *      - h3
 *      - h4
 *      - h5
 *      - h6
 *      - regular
 *      - regular bold
 *      - smal
 * REQUIREMENT: The font size must be calculated taking the base font-size of the page which is 14px,
 * and the corresponding size foreach font type should be calculated using "rem".
 * REQUIREMENT: Even if every type of Typography has their own size and color and weigth,...etc, this should be customizable
 * using props.
 * REQUIREMENT: The component must be typed using prop-types. 
 * REQUIREMENT: The component styles should be created using scss modules, also, the scss should use the architecture BEM.
 * REQUIREMENT: The component should be tested.
 * REQUIREMENT: The component should be described and integrated in story book.
 */

const TypographyComponent = () => {
	return <div>TypographyComponent</div>;
};

TypographyComponent.propTypes = {};

export default TypographyComponent;
